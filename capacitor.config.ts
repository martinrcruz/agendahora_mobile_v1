import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'agendahora.amsolutions.cl',
  appName: 'agendahora_app_v1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
